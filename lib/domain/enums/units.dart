enum TemperatureUnit {
  celsius,
  fahrenheit,
  kelvin,
}

enum SpeedUnit {
  metersPerSecond,
  kilometersPerHour,
  milesPerHour,
}

enum PressureUnit {
  hectoPascal,
  milliBar,
  millimeterOfMercury,
}
