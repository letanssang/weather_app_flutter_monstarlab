import 'dart:ui';

class WeatherColor {
  final Color startColor;
  final Color midColor;
  final Color endColor;
  final Color foregroundColor;
  const WeatherColor(
      this.startColor, this.midColor, this.endColor, this.foregroundColor);
}
